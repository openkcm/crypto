{{- if .Values.krypton.pod.disruptionBudget.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "krypton.fullname" . }}-krypton
  namespace: {{ include "krypton.namespace" . }}
  labels:
    {{- include "krypton.core.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "krypton.core.selectorLabels" . | nindent 6 }}
  {{- if not (or (hasKey .Values.krypton.pod.disruptionBudget "minAvailable") (hasKey .Values.krypton.pod.disruptionBudget "maxUnavailable")) }}
  minAvailable: 1 # Default value because minAvailable and maxUnavailable are not set
  {{- end }}
  {{- if (and (hasKey .Values.krypton.pod.disruptionBudget "minAvailable") (hasKey .Values.krypton.pod.disruptionBudget "maxUnavailable")) }}
  {{- fail "You can only set one of minAvailable or maxUnavailable in the PodDisruptionBudget" }}
  {{- end }}
  {{- if hasKey .Values.krypton.pod.disruptionBudget "minAvailable" }}
  minAvailable: {{ .Values.krypton.pod.disruptionBudget.minAvailable }}
  {{- end }}
  {{- if hasKey .Values.krypton.pod.disruptionBudget "maxUnavailable" }}
  maxUnavailable: {{ .Values.krypton.pod.disruptionBudget.maxUnavailable }}
  {{- end }}
{{- end }}
