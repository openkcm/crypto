{{- if .Values.enkrypton.pod.disruptionBudget.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "krypton.fullname" . }}-enkrypton
  namespace: {{ include "krypton.namespace" . }}
  labels:
    {{- include "krypton.enkrypton.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "krypton.enkrypton.selectorLabels" . | nindent 6 }}
  {{- if not (or (hasKey .Values.enkrypton.pod.disruptionBudget "minAvailable") (hasKey .Values.enkrypton.pod.disruptionBudget "maxUnavailable")) }}
  minAvailable: 1 # Default value because minAvailable and maxUnavailable are not set
  {{- end }}
  {{- if (and (hasKey .Values.enkrypton.pod.disruptionBudget "minAvailable") (hasKey .Values.enkrypton.pod.disruptionBudget "maxUnavailable")) }}
  {{- fail "You can only set one of minAvailable or maxUnavailable in the PodDisruptionBudget" }}
  {{- end }}
  {{- if hasKey .Values.enkrypton.pod.disruptionBudget "minAvailable" }}
  minAvailable: {{ .Values.enkrypton.pod.disruptionBudget.minAvailable }}
  {{- end }}
  {{- if hasKey .Values.enkrypton.pod.disruptionBudget "maxUnavailable" }}
  maxUnavailable: {{ .Values.enkrypton.pod.disruptionBudget.maxUnavailable }}
  {{- end }}
{{- end }}
